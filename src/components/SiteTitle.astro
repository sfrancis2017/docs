---
// Intentionally empty — site identity (logo + name) is rendered in the
// cross-site bridge nav above Starlight's default Header. This prevents
// the duplicate title from competing with the search box for space.
---
